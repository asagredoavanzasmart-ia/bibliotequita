/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Sello de build inyectado por vite.config.ts (define) — fecha/hora del build.
declare const __BUILD_TAG__: string;
